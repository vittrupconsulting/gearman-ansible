---

- ansible.builtin.apt:
    name: "redis"

- ansible.builtin.apt:
    name: "python3-pip"

- ansible.builtin.apt:
    name: "git"

- ansible.builtin.apt:
    name: "rsync"

- ansible.builtin.apt:
    name: "ansible"

- ansible.builtin.pip:
    name: "redis"

